__pycache__/
*.py[cod]
.venv/
venv/
data/*.db
*.db
!Latest Database/
!Latest Database/team_tracker.db
# Large local SQL dumps / recovery copies (do not push to Gerrit)
data/**/*.sql
data/**/*.sql.*
.env
.pytest_cache/
Secret/
Secrets/
Secret
*.pat
*.token

# Local DB snapshots (auto backup every N hours while app runs; see team_tracker_backup.py)
Backup/

# Local / legacy clutter consolidated under one folder (see Not Relevant Files/README.md)
Not Relevant Files/*
!Not Relevant Files/README.md

# Cursor / local IDE (not part of the app)
terminals/
mcps/
.cursor/
